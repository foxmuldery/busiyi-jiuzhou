# 主协调_BSI-UX-016 C-025 主线程验收记录

> 项目：《不思异：九州》Web Demo  
> 日期：2026-06-16  
> 验收对象：`BSI-C-025 下一轮最小实现`  
> 验收入口：`http://127.0.0.1:4178/03_WebDemo/prototype/`  
> 说明：4177 端口在本轮环境中监听异常，主线程临时使用 4178 进行浏览器验收。

## 结论

C-025 通过主线程阶段验收，可以作为当前试玩版本继续推进。

本轮已经验证：

- 页面能加载，标题为 `不思异：九州 Web Demo 原型`。
- 首屏不是空白，三资源、旅途舞台、补给和选择区均可见。
- 控制台无 `error / warn`。
- 起点事件可点击并真实改变资源。
- 九州图路线卡显示资源芯片、风险章、定遇/异象/补救信息。
- 选路后能触发半途路遇。
- 半途事件选择按钮显示动作章和资源芯片。
- 处理半途路遇后能抵达 `故王道`。
- 抵达后地点志包含 `arrivalText + detail`，日志出现 `抵达见闻`。
- 小横屏 `844x390` 下旅途页可见；地图页经主线程补丁后优先显示路线名、资源芯片、风险章和路遇信息。

## 主线程补丁

主线程验收时发现：`844x390` 地图路线卡中，路线缩略图会占用过多高度，风险章不够醒目。

已做最小样式修正：

- 在低高度横屏地图路线列表中隐藏路线缩略图。
- 隐藏长诗句、长路径和长 hint。
- 保留路线名、资源芯片、风险章、定遇/异象/补救、补给预览。
- 将 `index.html` 的脚本/样式版本号更新为 `20260616-c025b`，避免浏览器缓存旧样式。

涉及文件：

- `/Users/yuanzhe/Documents/game/GitHub资产区/03_WebDemo/prototype/styles.css`
- `/Users/yuanzhe/Documents/game/GitHub资产区/03_WebDemo/prototype/index.html`

## 验收路径

实际点击路径：

```text
加载页面
-> 点击起点事件「整点车具」
-> 打开九州图
-> 点击「沿旧王道向西缓行」
-> 触发半途路遇「当康啼垄」
-> 点击「随啼翻土」
-> 抵达「故王道」
```

观察到的关键状态：

```text
车轴 82 / 粮草 91 / 神志 85
地点：故王道
日志：抵达见闻：深车辙把车往西牵，像旧路还认得队伍。
地点志：arrivalText 与 detail 同屏显示
```

## 小横屏检查

尺寸：`844x390`

结果：

| 区域 | 结果 |
|---|---|
| 顶部资源 | 可见，三资源有 `安` 状态章 |
| 旅途舞台 | 可见，不遮挡底部操作 |
| 补给区 | 可见，补给按钮显示动作和资源芯片 |
| 事件选择 | 可见，按钮显示动作章和资源芯片 |
| 九州图 | 可见，地图节点只露出名称层级 |
| 路线卡 | 可见，缩略图已隐藏，资源芯片与风险章更靠前 |
| 控制台 | 无 `error / warn` |

## 阻断级风险扫描

主线程按 E-010 红线做了快速扫描：

- 未发现玩家可见 `Out There / Darkest Dungeon / FAR` 等竞品表达。
- 未发现星球、飞船、氧气、火把压力等竞品化关键词进入原型文本。
- 未发现资料站、百科、现代译文式文本。
- 本轮未导入新图片、音频、字体或外部库。
- `cleared` 只作为音频状态枚举存在，不是把候选素材标成正式可用。

结论：不阻塞内部 Demo 继续测试。

## 已跑检查

```text
node --check GitHub资产区/03_WebDemo/prototype/app.js
node --check GitHub资产区/03_WebDemo/prototype/data.js
git diff --check -- GitHub资产区/03_WebDemo/prototype/styles.css GitHub资产区/03_WebDemo/prototype/index.html
```

结果均通过。

## 剩余风险

- 本轮只做了浏览器内置环境验收，尚未在真实手机横屏触控上试。
- `844x390` 路线卡内部仍保留隐藏长文本供完整语义使用；视觉上已优先显示关键选择信息。
- 事件音乐和新短音效仍未接入正式候选，继续由 F 线程处理试听和裁短。
- E 线程的正式 C-025 后复核还可在下一轮按模板补完整表，但当前没有阻断项。

## 主线程后续动作

1. 继续以 C-025b 作为当前试玩版本。
2. 下一步让 E 线程按实际 C-025b 填 E-010 复核表。
3. F 线程继续做音乐试听工作台或试听结论。
4. 主线程可把 `http://127.0.0.1:4178/03_WebDemo/prototype/` 作为本轮临时验收入口。

