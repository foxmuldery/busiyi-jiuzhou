# BSI-UX-026 横屏遭遇弹窗密度优化回执

日期：2026-06-21  
负责：主协调 / C 线小修  
目标：把遭遇详情从偏“网页长弹窗”压回横屏手游中景弹窗，事件图、诗引、正文、选项都跟随同一个居中弹窗展示，且选项一次性可见。

## 已完成

- `styles.css`：在 900x500 一类小横屏下，将遭遇/路遇弹窗改成 820px 宽、左图右文、底部选项的紧凑布局。
- `styles.css`：诗引压到标题线右侧，正文限制 5 行，事件图保持可见，3 个选项在弹窗底部横向排布。
- `index.html`：样式缓存号更新为 `20260621-c103`，避免浏览器继续加载旧版样式。
- `qa-check.js`：新增 `compact landscape story modal density contract`，后续如果弹窗重新变成长页面，会被自动检查拦住。

## 验收结果

- `node --check GitHub资产区/03_WebDemo/prototype/qa-check.js`：通过。
- `node --check GitHub资产区/03_WebDemo/prototype/app.js`：通过。
- `node GitHub资产区/03_WebDemo/prototype/qa-check.js`：全部通过。
- 浏览器复验：900x500 横屏，从中原驿走到故王道，再走雷泽浅畔，打开 `泽鼓回雷` 遭遇弹窗。

## 浏览器证据

测试地址：

`http://127.0.0.1:4181/GitHub%E8%B5%84%E4%BA%A7%E5%8C%BA/03_WebDemo/prototype/index.html?fresh=1&verify=modal-density-c103-1782013527025`

实测状态：

- 地点：雷泽浅畔。
- 弹窗：`泽鼓回雷`。
- 选项数：3 个，全部在同一弹窗内一次性可见。
- 面板尺寸：约 820 x 289 px。
- 垂直溢出：无。
- 控制台错误/警告：无。

## 后续建议

下一步不要继续扩大弹窗信息量，应优先做两件小事：

1. 把所有遭遇图逐步替换成方图/近方图，避免宽屏图压缩后主体太小。
2. 给事件弹窗加一套“状态反馈小浮层”，例如资源涨跌、神志异动、地点志收录，减少正文里解释性的文字。
