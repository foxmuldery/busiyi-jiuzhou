# 主协调 BSI-UX-091 线上小横屏首分钟实测回执

日期：2026-06-24

## 1. 本轮目标

在不等待外部真人反馈的情况下，补一轮线上真实渲染验证：确认固定公开入口和游戏首分钟在手机横屏尺寸下不是只“机器通过”，而是玩家真的能看到入口、复制任务、处理第一个遭遇并进入下一步。

## 2. 测试环境

- 线上入口：<https://webdeploy-green.vercel.app/>
- 当前部署：`dpl_HJ7UwPQRCrJgi4kEmmNtfY8bTHut`
- 浏览器：Codex 内置浏览器
- 视口：`844 x 390`
- 测试路径：公开入口 -> 复制 5 分钟任务 -> 开始试玩 -> 第一遭遇选项 -> 结果弹窗 -> 轻点关闭

## 3. 验收结果

- 公开入口：
  - 页面标题为 `不思异：九州 试玩`。
  - 页面非空白，显示 `不思异：九州`、试玩目标、`开始试玩`、`复制 5 分钟任务`、`复制听音任务`、`复制反馈模板`。
  - 控制台 `error/warn` 为 0。
  - 点击 `复制 5 分钟任务` 时，浏览器未放行剪贴板，但自动展开手动复制文本。
  - 手动复制文本完整，包含：
    - `《不思异：九州》5 分钟试玩任务`
    - `试玩密码：tusun2026`
    - `https://webdeploy-green.vercel.app/play/`
    - 结局 `复制反馈包` 指引。
- 游戏首屏：
  - 成功进入 `/prototype/index.html?fresh=1`。
  - 控制台 `error/warn` 为 0。
  - 视口实际为 `844 x 390`，小横屏媒体规则已触发。
  - 页面无横向或纵向滚动溢出。
  - 顶部资源条一次性可见：`车轴 80`、`粮草 90`、`神志 85`。
  - `下一步：处理遭遇` 可见。
  - 第一遭遇弹窗 `驿门未闭` 可见。
  - 两个选项一次性显示在视口内：`整点车具`、`立刻上路`。
- 交互闭环：
  - 点击 `立刻上路` 后出现 `等待落账`。
  - 落账后粮草从 `90` 变为 `88`。
  - 下一步变为 `补给一次`。
  - 结果弹窗显示 `轻点继续`。
  - 点击弹窗外区域后弹窗关闭，补给选项可见。

## 4. 注意事项

- 截图接口本轮超时，未形成图片证据；本轮以 DOM、布局尺寸、控制台日志和交互状态为验收证据。
- 这次只证明线上小横屏首分钟链路可用，不能替代真实玩家的主观反馈。
- P0 总状态仍是：机器验收通过，待真人确认；还需要真实 5 分钟试玩反馈和主观听音结论。
