# BSI-UX-022｜山海P0遭遇与后段压力小修回执

日期：2026-06-20  
负责人：主线程协调  
范围：Web Demo 内部试玩版

## 1. 本轮已完成

### 1.1 山海经亮点事件补强

已把 3 个 P0 事件扩成更有“山海志异”味道的三选项结构：

| 事件 | 地点 | 本轮补强点 |
|---|---|---|
| `black_trade` | 黑齿市 | 增加“影子称粮”的异族交易感，并增加第 3 选择 |
| `qingqiu_lamps` | 青丘外邑 | 增加“空册问名/不留名”的狐灯民俗，并增加第 3 选择 |
| `nameless_prayer` | 无名祭所 | 增加不拜直过的选择，强化祭祀禁忌 |

浏览器已实测青丘外邑新版遭遇：

- 文本出现“空册”。
- 选项出现 3 个：`换狐灯静夜`、`留假名入册`、`以粮换无名路引`。
- 地点补给差异化存在：青丘外邑显示 `定神` 与 `取粮`，没有车轴补给。
- 音乐 HUD 跟随切到 `CORE-006 青丘外邑`。

### 1.2 后段路线压力与保底

已做轻量后段压力调整：

- `market_to_stele` 神志消耗提高。
- `stele_to_rift` 车轴、粮草、神志压力提高。
- `feather_to_dream` 粮草、神志压力提高。
- `dream_to_rift` 车轴、粮草、神志压力提高。

同时增加低资源补救触发：

- 当任意核心资源低于或等于 15 时，路线随机事件会优先从补救型事件池抽取。
- 这不是强行送资源，而是避免玩家因为一次随机坏运气直接进入死局。

### 1.3 缓存修复

入口页已把 `data.js` 版本号更新为：

```html
data.js?v=20260620-b013
```

避免浏览器继续加载旧事件数据。

## 2. 已通过验证

### 2.1 自动检查

已通过：

- `qa-check.js`
- `journey-smoke-check.js`
- 本轮相关文件 `git diff --check`

关键检查包括：

- 游戏数据完整。
- 路线端点有效。
- 地点事件引用有效。
- 补给差异合法。
- 地点/事件图片引用合法。
- 音乐 HUD 与当前曲目入口存在。
- P0 山海遭遇扩展契约通过。
- 低资源补救事件契约通过。
- 选择项一屏显示预算仍通过。

### 2.2 浏览器手动链路

测试地址：

```text
http://127.0.0.1:4181/GitHub%E8%B5%84%E4%BA%A7%E5%8C%BA/03_WebDemo/prototype/index.html?fresh=1&verify=shanhai-b013-1781937840297
```

横屏测试尺寸：`900 x 500`

已走通链路：

```text
开局事件：驿门未闭
-> 选择：立刻上路
-> 地图页
-> 选择路线：中原驿 -> 青丘外邑
-> 半途路遇：九尾灯影
-> 选择：挂灯行夜
-> 抵达：青丘外邑
-> 打开地点遭遇：狐灯绕名
```

浏览器复验结果：

- 页面标题正确。
- 页面非空白。
- 未出现框架错误遮罩。
- 控制台无 error/warn。
- 路线选择、路遇弹窗、结果轻点继续、地点遭遇入口均可操作。
- 截图接口连续超时，未作为阻塞项；DOM 与交互链路已验证。

## 3. 当前平衡状态

最近一次平衡模拟结论：

| 策略 | 成功率 | 警告率 | 补救率 | 备注 |
|---|---:|---:|---:|---|
| balanced | 100% | 约 10% | 0% | 压力刚到目标下沿 |
| novice | 100% | 约 83.7% | 约 1% | 会明显告急，但没有硬死局 |

主线程判断：

- 目前适合继续保留这个压力区间。
- 不宜马上再大幅加难，否则新手路径可能从“紧张”变成“沮丧”。
- 下一步应该增加更多事件内容，而不是继续只调数值。

## 4. 已派发给子线程

| 线程 | Agent | 本轮任务 |
|---|---|---|
| A 数值/机制 | Laplace | 输出后段压力与保底复核建议，不直接改代码 |
| B 世界观/事件 | Dewey | 输出下一批 8 个山海事件与配图文字包 |
| C UI/技术 | Euclid | 输出小横屏 UI 下一轮修复建议，不直接改代码 |
| D/F 视觉音频 | Curie | 输出事件方图、绿幕动态物料、音效提示语 |

## 5. 下一步建议

下一轮最小可验证步骤：

1. 等子线程回传文档。
2. 主线程审核后，只合并一批最小内容：优先 2 个地点事件 + 1 个 UI 触屏小修 + 1 组事件方图提示语。
3. 再跑同一条 900 x 500 横屏链路，确认“内容增加后不挤、不乱、不死局”。

