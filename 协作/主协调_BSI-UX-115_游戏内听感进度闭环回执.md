# 主协调 BSI-UX-115 游戏内听感进度闭环回执

> 日期：2026-06-25  
> 负责人：主协调 / C/F  
> 状态：已验收并已部署  

## 1. 本轮目标

P0 仍缺“声音主观听感”人工确认。此前游戏内设置面板可以试听并标记音乐，但测试者不容易看出还差几首。本轮把听感标记做成明确进度闭环。

## 2. 已完成内容

1. 设置面板新增 `听感进度` 条，显示已标记音乐数和总数。
2. 未完成时显示还差几首，并列出未评音乐 ID。
3. 全部标记后切换为完成状态。
4. 复制听感模板新增 `听感进度：x/y` 行，方便主线程判断材料是否齐全。
5. 调试接口新增 `audioReviewProgress`、`getAudioReviewProgress` 和 `getAudioReviewTemplateText`，便于后续机器/浏览器复核。
6. 更新 `styles.css` / `app.js` 缓存号到 `20260625-c166`，避免浏览器继续使用旧脚本。
7. QA 守门新增听感进度 UI、模板字段、调试接口和缓存号检查。

## 3. 验收结果

- `node --check app.js`：通过。
- `qa-check.js`：通过。
- `p0-readiness-check.js --write-report`：机器验收 9/9，通过；待人工 2 项。
- 浏览器实测：设置面板初始显示 `听感进度 0/6`；标记第一首为“保留”后显示 `听感进度 1/6`，并列出剩余 5 首未评。
- 线上冒烟：12/12 通过，阻断项 0。

## 4. 线上版本

- 公开入口：https://webdeploy-green.vercel.app/
- 试玩密码：`tusun2026`
- 部署 ID：`dpl_9VzzZganmyqdojfkGpuY3Dm3HdDJ`

## 5. 后续建议

下一步可把外部 `P0音频听感验收工作台.html` 的进度摘要和游戏内听感进度做口径统一：同样显示 `x/y`，并把“保留 / 待改 / 弃用”的数量拆出来。
