# BSI-UX-110 人工反馈收件台试玩编号分组回执

日期：2026-06-25

## 结论

已把 `P0人工反馈收件台.html` 从“给每份反馈生成收件编号”升级为“按同一局 `JZ-` 试玩编号归档”。主线程收到多份聊天反馈、统一反馈包、结局复盘或音频摘要时，可以先贴入收件台，由它抽取试玩编号、按局分组、标出缺编号反馈，再复制收件汇总给主线程判断缺口。

- 公开试玩入口：`https://webdeploy-green.vercel.app/`
- 试玩密码：`tusun2026`
- 最新生产部署：`dpl_EAmAzy1CjMbUpv7ESjaYGRRZarcR`

## 本轮完成

1. 收件台新增 `extractRunCodes`，自动抽取粘贴文本里的 `JZ-` 试玩编号。
2. 每条反馈同时保留：
   - 收件编号：`P0-FB-*`
   - 试玩编号：`JZ-*`
3. 收件箱统计新增 `局数`，按唯一试玩编号统计当前回收了多少局反馈。
4. 每张反馈卡显示试玩编号；没有编号时显示 `缺试玩编号`。
5. `复制本条补收消息` 会在缺编号时自动追加 `补同一局的 JZ 试玩编号`。
6. `复制收件汇总` 新增：
   - `按试玩编号分组`
   - `缺试玩编号`
   - 每条明细的 `试玩编号`
7. 线上冒烟新增 `P0 人工反馈收件台`，范围从 11 页扩展到 12 页。
8. P0 总验收报告同步为：收到多份反馈时，先贴入反馈收件台，按 `JZ` 试玩编号分组后再判断缺口。

## 验收证据

- 本地语法检查：
  - `node --check online-smoke-check.js`
  - `node --check p0-readiness-check.js`
  - `node --check qa-check.js`
- 格式检查：本轮相关文件 `git diff --check` 通过。
- 本地 QA：`qa-check.js` 全部通过。
- P0 总验收：机器验收 `9/9`，待人工 `2`，需处理 `0`。
- 线上冒烟写回：`12/12` 页面通过，阻断项 `0`。
- 最终部署后复查：`online-smoke-check.js` 第二次通过，`12/12` 页面通过，阻断项 `0`。
- 直接抽检 `/play/`：HTTP `200`，`x-robots-tag: noindex`，密码门和干净新局片段存在。

## 已知限制

P0 最终完成仍需真实玩家 5 分钟试玩反馈和主观听音摘要。机器验收能证明反馈回收链路可用，但不能替代真人手感和音乐听感判断。

