# 主协调 BSI-UX-094 线上默认视口核心链路复验回执

日期：2026-06-24

## 1. 本轮目标

在机器 QA 之外，再用内置浏览器走一遍线上默认视口真实渲染链路，确认公开入口、首个遭遇、资源落账、结果弹窗、P0 总报告和公开入口听音任务兜底都能实际使用。

## 2. 测试环境

- 固定入口：<https://webdeploy-green.vercel.app/>
- 当前部署：`dpl_HJ7UwPQRCrJgi4kEmmNtfY8bTHut`
- 试玩密码：`tusun2026`
- 浏览器：Codex 内置浏览器
- 实际视口：`1280 x 720`
- 测试路径：
  - 公开入口 -> 开始试玩 -> 首个遭遇 -> 选择 `立刻上路` -> 结果弹窗 -> 关闭弹窗 -> 补给状态
  - 公开入口 -> `复制听音任务`
  - P0 总验收报告页

## 3. 验收结果

- 公开入口：
  - 页面标题为 `不思异：九州 试玩`。
  - 页面非空白，显示 `开始试玩`、`打开完整测试入口`、`复制 5 分钟任务`、`复制听音任务`、`复制反馈模板`。
  - 控制台 `error/warn` 为 0。
- 游戏首段：
  - 成功进入 `index.html?fresh=1`。
  - 页面标题为 `不思异：九州 Web Demo 原型`。
  - 首个遭遇 `驿门未闭` 可见。
  - 两个选项 `整点车具`、`立刻上路` 可见。
  - 点击完整无障碍名按钮 `取 立刻上路 粮草-2` 后，粮草从 `90` 落账到 `88`。
  - 下一步变为 `补给一次`。
  - 结果弹窗出现 `轻点继续`，关闭后补给按钮 `修轴`、`取粮` 可见。
  - 控制台 `error/warn` 为 0。
- 音频入口：
  - 选择遭遇后，页面显示 `声场已启`，音乐状态进入 `播放中`。
  - 这说明玩家首次交互后，声音层能被浏览器允许启动。
- 公开入口听音任务：
  - `复制听音任务` 按钮在可见 DOM 中存在。
  - Browser/Playwright role 点击通道本轮仍出现超时，但可见 DOM 节点点击成功。
  - 浏览器未放行剪贴板时，页面自动展开手动复制文本。
  - 兜底文本包含：
    - `《不思异：九州》P0 3 分钟听音任务`
    - `试玩密码：tusun2026`
    - `请只判断听感，不判断授权`
    - `先听完 6 首音乐候选`
  - 控制台 `error/warn` 为 0。
- P0 总验收报告：
  - 线上报告页返回正常，标题为 `不思异：九州 P0 试玩版总验收报告`。
  - 报告显示 `机器验收 6/6`、`待人工 2`、`需处理 0`。
  - 报告包含 `3 分钟听音任务`、`复制听音任务`、`P0 音频听感验收摘要` 和密码 `tusun2026`。
  - 控制台 `error/warn` 为 0。

## 4. 注意事项

- 本轮尝试设置 `844 x 390` 小横屏视口，但 Browser 视口覆盖没有反映到页面，实际仍为 `1280 x 720`。因此本回执不作为小横屏新证据；小横屏证据仍以 BSI-UX-091 为准。
- 本轮截图接口 `Page.captureScreenshot` 超时，未形成图片证据；本回执以 DOM、交互状态、资源数值和控制台日志为证据。
- P0 仍未最终完成，因为还缺真实玩家 5 分钟试玩反馈和主观听音结论。
