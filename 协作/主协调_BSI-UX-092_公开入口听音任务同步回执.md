# 主协调 BSI-UX-092 公开入口听音任务同步回执

日期：2026-06-24

## 1. 本轮目标

按用户确认，公开试玩密码固定为 `tusun2026`。在不改变玩法和 UI 主体的前提下，把 P0 音频听感任务同步到固定公开入口，让外发测试时可以直接从根页复制听音任务，减少“声音还要单独找内部验收台”的摩擦。

## 2. 已完成

- 固定公开入口新增 `复制听音任务`。
- 根页现在同时提供：
  - `复制 5 分钟任务`
  - `复制听音任务`
  - `复制反馈模板`
- 听音任务内容包含：
  - 听音入口：线上 `P0音频听感验收工作台.html`。
  - 试玩密码：`tusun2026`。
  - “只判断听感，不判断授权”的边界提醒。
  - 先听完 6 首音乐候选，再听环境、神志、告急、短音和地图层。
  - 回答 5 个主观问题，并回传 `P0 音频听感验收摘要`。
- 剪贴板被浏览器拦截时，沿用公开入口的手动复制兜底。
- `qa-check.js` 已加入公开根入口听音任务守门项。
- `README.md` 已同步说明：需要单独判断声音时，可从公开入口复制 3 分钟听音任务给听音人。

## 3. 验收结果

- `node GitHub资产区/03_WebDemo/prototype/qa-check.js`：全部通过。
- `node GitHub资产区/03_WebDemo/prototype/build-webdeploy-package.js`：已重新生成 `output/webdeploy`。
- `node GitHub资产区/03_WebDemo/prototype/p0-readiness-check.js`：`P0 机器验收通过，待真人确认；机器验收 6/6；待人工 2；需处理 0`。
- 已部署到 Vercel 生产环境：
  - 最新部署：`dpl_HJ7UwPQRCrJgi4kEmmNtfY8bTHut`
  - 固定试玩入口：<https://webdeploy-green.vercel.app/>
  - 试玩密码：`tusun2026`
- 线上根页抽检通过：
  - 根入口返回 200。
  - 响应包含 `copyAudioTask`、`复制听音任务`、`《不思异：九州》P0 3 分钟听音任务`、`请只判断听感，不判断授权`、`先听完 6 首音乐候选`、`复制音频验收摘要` 和 `试玩密码：tusun2026`。
  - 响应带 `X-Robots-Tag: noindex`。
- 内置浏览器验证：
  - DOM 可见 `复制听音任务` 按钮。
  - Browser/CDP 点击通道本轮超时，未能把“真实点击复制”作为浏览器证据。
  - 静态合约已复核：按钮、`copyText(audioTask...)`、剪贴板写入和手动复制兜底均存在。

## 4. 当前判断

公开入口现在可以同时外发真人 5 分钟试玩任务和 3 分钟听音任务，密码统一为 `tusun2026`。这一步仍不替代真实玩家和听音人的主观反馈；P0 总状态仍是机器验收通过、待真人确认。
