# BSI-UX-027 结果资源反馈与旧提示保护回执

日期：2026-06-21  
负责：主协调 / C 线小修  
目标：让玩家点完选项后立刻看见“本次变化”，减少纯文字解释；同时修掉首屏延迟提示在玩家已处理遭遇后仍然冒出的旧提示问题。

## 已完成

- `index.html`：新增 `storyModalOutcome` 结果摘要容器，入口缓存号更新到 `20260621-c105`。
- `app.js`：事件、路遇、补救结果弹窗会显示“本次变化”资源芯片。
- `app.js`：补给、路线、事件、路遇、补救的舞台浮层可携带资源变化芯片和短摘要。
- `app.js`：首屏“第一步”提示在延迟触发前会重新检查当前遭遇是否仍未处理，避免处理后还提示“先处理当前遭遇”。
- `styles.css`：补齐结果摘要行和舞台浮层资源芯片样式，小横屏下不撑高弹窗。
- `qa-check.js`：新增 `outcome feedback visual contract` 和 `opening hint stale guard contract`。

## 验收结果

- `node --check GitHub资产区/03_WebDemo/prototype/app.js`：通过。
- `node --check GitHub资产区/03_WebDemo/prototype/qa-check.js`：通过。
- `node GitHub资产区/03_WebDemo/prototype/qa-check.js`：全部通过。

## 浏览器证据

测试地址：

`http://127.0.0.1:4181/GitHub%E8%B5%84%E4%BA%A7%E5%8C%BA/03_WebDemo/prototype/index.html?fresh=1&verify=outcome-feedback-c105-1782014530226`

实测 1：首个遭遇 `立刻上路`

- 结果弹窗标题：`立刻上路`。
- 结果行：`本次变化 粮-2`。
- 结果行资源芯片数：1。
- 旧的“第一步 / 先处理当前遭遇”提示：未残留。
- 弹窗垂直溢出：无。
- 控制台错误/警告：无。

实测 2：中原驿补给 `清点驿粮`

- 舞台浮层：`中原驿 补给完成 粮+8`。
- 舞台浮层资源芯片数：1。
- 顶部粮草资源同步变化到 96，并有 `+粮8` 飘字。
- 控制台错误/警告：无。

## 后续建议

下一步可以继续沿这个方向做“非文字化状态反馈”：

1. 路线出发和半途路遇也补一组更明显的行进/停下反馈。
2. 神志变化时把弹窗边框、污染层和短音效联动得更明显。
3. 结局前的最后一段路增加一次更强的仪式感浮层。
