# 主协调 BSI-UX-105 首批发测反馈回收量化回执

## 结论

- 固定公开试玩入口：`https://webdeploy-green.vercel.app/`
- 试玩密码：`tusun2026`
- 最新生产部署：`dpl_8CADUPENPHU5o5wJXB7kaqR3Sy2V`
- 当前 P0 状态：机器验收通过，仍待真实 5 分钟试玩和主观听音反馈。

## 本次完成

1. 强化 `P0发测批次台.html`。
   - 新增 `任务类型`：`5分钟试玩+听音 / 只做5分钟试玩 / 只做3分钟听音 / 内容/山海氛围反馈`。
   - 新增 `目标反馈数`。
   - 新增 `已回收反馈`。
   - 新增 `还差反馈`统计。
   - 批次邀请会带上任务类型和目标回收数。
   - 批次追收会带上还差反馈数。
   - 发测汇总会显示目标反馈、已回收反馈和还差反馈。

2. 强化线上守门。
   - `online-smoke-check.js` 从 9 页扩展到 10 页。
   - 新增对 `P0 发测批次台` 的线上检查。
   - 检查字段包括密码门、复制本批邀请、任务类型、目标反馈数、已回收反馈和还差反馈。

3. 同步报告与说明。
   - `p0-readiness-check.js` 的人工收口路径已更新为登记任务类型、目标反馈数、已回收反馈。
   - `P0试玩版总验收报告.md/html` 已显示线上冒烟 `10/10`。
   - `README.md` 已同步发测前记录口径。

## 验收结果

- `qa-check.js`：全部通过。
- `p0-readiness-check.js --write-report`：机器验收 `9/9`，待人工 `2`，需处理 `0`。
- `build-webdeploy-package.js`：发布包生成，`157` 个文件。
- `online-smoke-check.js --write-report`：线上 `10/10`，阻断项 `0`。
- `visual-smoke-check.js --write-report`：截图 `2/2`，阻断项 `0`。
- 最终线上抽检：
  - `P0发测批次台.html` 包含密码门。
  - `P0发测批次台.html` 包含任务类型、目标反馈数、已回收反馈、还差反馈。
  - `p0-readiness-status.js` 显示 `页面 10/10，阻断项 0`。

## 下一步

1. 用 `P0发测批次台.html` 建立第一批真实测试者。
2. 每批填写目标反馈数，发出后根据已回收反馈更新还差数量。
3. 追收到的材料进入 `P0人工反馈收件台.html` 编号。
4. 满足材料后进入 `P0真人试玩验收工作台.html`，生成最终人工验收摘要。
